spring:
  datasource:
    url: jdbc:h2:mem:testdb

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true

  h2:
    console:
      enabled: true
  liquibase:
    enabled: true
  messages:
    basename: i18n.messages